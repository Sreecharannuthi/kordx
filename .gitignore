*.iml
.gradle
local.properties
.idea/*
!/.idea/codeStyles
/.idea/codeStyles/*
!/.idea/codeStyles/Project.xml
!/.idea/codeStyles/codeStyleConfig.xml
!/.idea/dictionaries
*.iml
.DS_Store
build
captures
*.apk
*.aab
.externalNativeBuild
.cxx
local.properties
node_modules
dist
secrets
.kotlin

# Release signing config + the actual keystore (never commit real secrets).
# `app/keystore.properties.example` IS committed as a template.
/app/keystore.properties
/app/keystore/

# local test audio (not part of the build)
/sample
/kordx-music

# release verification scratch files
/release_build.log
/build_release_verify.*
/dexdump_tmp/
/error.jpg
/fix-verified.png

# local env helper
/.env.sh
/screen

# specs/ — local working docs/planning, excluded from the committed project (everything under specs/)
specs/*

# root pointer files — local-only (pointing into gitignored specs/)
AGENTS.md
CLAUDE.md
CONVENTIONS.md

# Cleanup-inventory scratch files (created by the spec author, not part of the build)
__inventory_*.txt
__inventory_*.csv
__*.ps1
__*.txt

# Debug screenshots, uiautomator dumps, temp databases
avd-*.png
ui*.xml
*.db
cover-test.jpg
error-screenshot.png

